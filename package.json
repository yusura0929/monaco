{
  "name": "kintone-customize",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --outfile=dist/kintone-customize.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
